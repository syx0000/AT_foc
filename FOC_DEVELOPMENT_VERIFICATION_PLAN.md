# FOC 重构开发与验证计划

> 文档状态：草案，随架构评审更新。
>
> 适用工程：`E:\src\AT_foc_hall`
>
> 参考工程：`E:\src\AT_foc_hall - 副本`（只读）
>
> 设计依据：`重构.md`、`FOC_REFACTOR_DESIGN.md`

## 1. 目标与范围

本计划用于把当前裸外设工程逐步建设为可验证的 Hall FOC 控制工程。每个阶段必须独立完成开发、编译、静态检查和上板验证，禁止一次性迁移旧工程全部代码。

本阶段包含：

- WorkBench 可重复生成边界。
- BSP 电机硬件端口层。
- PWM安全控制。
- ADC电流、母线电压和温度采样。
- Hall状态、方向、速度和电角度估计。
- Clarke、Park、反Park、PI、SVPWM。
- 受限开环、电流环、速度环和位置环。
- 必要的状态机、故障保护和参数校准。
- 固定Flash区域、可扩展参数格式和A/B双槽掉电保护。

本阶段不包含：

- CAN协议和对象字典。
- OTA业务功能的迁移或重构。
- 自动电机参数辨识。
- 带宽自动测试。
- 旧调试命令全集。
- 与当前闭环无关的旧故障、兼容层和历史接口。

## 2. 强制开发规则

1. WorkBench生成文件只在 USER CODE 区增加稳定入口调用；人工业务代码使用新增文件。
2. 文件、函数和变量使用 `lower_snake_case`；类型使用 `lower_snake_case_t`；宏和枚举使用带模块前缀的 `UPPER_SNAKE_CASE`。
3. 每个新增文件包含 `@file`、`@brief`、`@author`、`@date` 和 `@version` 文件头。
4. 旧代码只能按功能迁移，不允许直接复制整个文件后再整理。
5. 每个迁移功能必须记录旧工程来源、保留原因和验证方法。
6. 未确认的硬件参数不得猜测；使用占位值时必须阻止电机进入运行状态。
7. 10 kHz中断内禁止动态内存、阻塞等待、Flash、通信发送和格式化打印。
8. Flash擦写只能在电机停止、PWM关闭、门极关闭的状态执行。
9. 每个关键节点验证通过后先更新 `README.md`，再提交。
10. 上板测试逐级增加电压、电流和速度限制；不得跳过低能量验证。

## 3. 验证环境与记录

### 3.1 必要设备

- 当前AT32F456CEU7控制板。
- 限流直流电源，具有可读输出电压和电流。
- 示波器，至少4通道更佳。
- 差分探头或适合母线/相节点测量的安全探头。
- 电流探头；没有时使用板载采样配合限流电源交叉检查。
- 调试器及Keil环境。
- 可自由转动的目标电机。
- 必要时准备机械空载或可控负载。

### 3.2 每次上板记录

每次验证至少记录：

```text
固件Git提交：
硬件版本：
电机型号/编号：
母线电压：
电源限流：
测试模式：
关键参数：
示波器截图/日志路径：
结果：通过/失败
异常与后续动作：
```

没有对应Git提交或明确工作区状态的测试结果，不作为回归基准。

## 4. 总体验证顺序

```text
M0 裸工程基线
 ↓
M1 BSP时间与安全输出
 ↓
M2 ADC采样与零偏
 ↓
M3 Hall反馈
 ↓
M4 FOC数学内核
 ↓
M5 受限开环
 ↓
M6 电流闭环
 ↓
M7 速度闭环
 ↓
M8 位置闭环
 ↓
M9 状态机与必要故障
 ↓
M10 Flash参数存储
 ↓
M11 系统回归与整理
```

每个阶段的入口条件必须全部满足，才允许进入下一阶段。

## 5. M0：裸工程与架构基线

### 5.1 入口条件

- `refactor/foc-kernel` 分支已建立并有远程基线。
- 旧工程完整保留且只读。
- 架构、命名规范和范围已评审。

### 5.2 开发内容

- 建立 `project/bsp`、`project/foc/foc_kernel`、`project/foc/foc_app` 文件结构。
- 建立 `project/config/product_version.h`，集中定义固件版本、目标硬件版本和兼容范围。
- 建立 `motor_log.h/.c`，提供0~4运行时日志等级，底层复用USART1 `printf`。
- 建立Keil分组与包含路径。
- 创建公共类型和模块空接口，但不迁移旧算法。
- `main.c` USER CODE区接入 `motor_app_init()` 和 `motor_app_poll()` 空实现。
- 中断文件暂不接入控制算法。
- 检查并排除CMSIS换行符等无意义修改。

### 5.3 验证方法

- Keil全量重新编译，不使用旧目标文件。
- 检查工程中不存在 `foc_fast` 路径或已删除文件引用。
- 检查固件版本、README记录和目标硬件版本一致。
- 分别设置日志等级0~4，确认各级输出过滤关系符合ERROR、WARN、DEBUG、INFO定义。
- 冷启动、硬件复位和调试器直接运行时，确认启动日志均打印产品名、FW、HW和`APP: OK`。
- 检查10 kHz、Hall及紧急关断ISR内不存在 `printf`、`LOGE/W/D/I`。
- 搜索新增FOC文件，确认不包含AT32寄存器头文件。
- 运行 `git diff --check`。
- 上电确认主循环运行且PWM保持安全状态。

### 5.4 通过标准

- 0 error；warning数量记录并评审，新增代码不得产生warning。
- WorkBench生成文件仅USER CODE区出现人工入口。
- 未使能门极，三相PWM无有效驱动。
- 工程复位、下载和重复上电正常。
- 日志等级越界设置不改变当前等级；启动日志完整且不因串口发送时机被截断。

### 5.5 中断频率实测记录（2026.09.03.2）

```text
SysTick                 1000 Hz   通过
TMR1 Channel4          20002 Hz   目标20000 Hz，通过
ADC快速预注入完成       10001 Hz   目标10000 Hz，通过
ADC慢速普通组完成        1000 Hz   与DMA中断重复，已关闭
ADC慢速DMA完成           1000 Hz   目标1000 Hz，通过
ADC触发失败                  0     通过
```

DMA1 Channel3已配置4个半字循环缓冲区。关闭ADC普通组完成中断后复测结果为 `adc_fast≈10000`、`adc_slow_dma=1000`、`adc_fail=0`，确认慢速ADC链路由DMA完成事件单独承载。

### 5.6 提交节点

```text
refactor: establish motor control architecture skeleton
```

## 6. M1：时间基准与PWM安全链路

### 6.1 开发内容

新增：

```text
project/bsp/motor_timebase.c/.h
project/bsp/motor_pwm_port.c/.h
project/bsp/motor_board_config.h
```

实现：

- DWT计数器初始化、周期读取和周期到微秒转换。
- TMR1周期读取。
- 三相比较值边界检查和写入。
- PWM正常使能、正常关闭和紧急关闭。
- EN_GATE安全控制。
- 初始化失败时保持PWM和门极关闭。
- 对当前 `wk_tmr1_init()` 自动开放输出的问题建立确定的关闭措施。

### 6.2 不上电检查

- `motor_pwm_port_init()` 默认关闭PWM和EN_GATE。
- 任意非法比较值被钳位，不发生计数器溢出。
- `motor_pwm_port_emergency_stop()` 不依赖状态机或动态状态。
- 初始化顺序中不存在门极先开启、控制器后初始化的窗口。

### 6.3 上板步骤

1. 不接电机或保持低压限流。
2. 示波器监测EN_GATE、三相高低桥PWM。
3. 上电、复位、下载、看门狗复位各执行多次。
4. 在PWM关闭状态检查所有门极输出。
5. 使用固定10%、50%、90%测试命令分别观察三相PWM。
6. 调用紧急关闭，测量从软件动作到门极关闭的延迟。
7. 用DWT测量已知延时，确认时间换算正确。

### 6.4 通过标准

- 上电及复位期间EN_GATE保持无效，PWM不会产生意外有效脉冲。
- 三相频率、中心对齐模式、互补输出和死区符合WorkBench配置。
- 比较值与实际占空比对应，0%和100%边界按安全策略处理。
- 紧急关闭可重复执行且结果一致。
- DWT时间误差满足调度和Hall测速需求，记录实测误差。

### 6.5 当前验证记录（2026.09.03.3）

- DWT测量10 ms已知延时结果正常，确认192 MHz周期到微秒换算有效。
- ADC快速中断接入通用性能统计，频率实测为10 kHz。
- ADC快速中断常态耗时104 cycles（约0.54 us），区间峰值134 cycles（约0.70 us），峰值周期占用约0.69%。
- 启动阶段DRV检查实测 `nFAULT=1`、`BIF=0`、`ready=1`、`MOE=0`，安全关闭状态符合预期。
- 固定50%三相PWM测试代码保持 `if (0)` 关闭，实际PWM波形、互补输出、死区和紧急关断延迟尚未验证。

### 6.6 提交节点

```text
feat: add motor timebase and safe pwm port
```

## 7. M2：ADC采样与零偏校准

### 7.1 开发内容

新增：

```text
project/bsp/motor_adc_port.c/.h
project/foc/foc_app/motor_calibration.c/.h
```

实现：

- ADC1/ADC2注入组两相电流读取。
- ADC普通组母线电压和温度读取。
- 原始值结构和采样计数。
- 电流零偏采样、平均、范围检查和应用。
- ADC触发失败、超时和数据越界诊断。
- 初始阶段保持原始计数接口；硬件比例确认后再增加物理单位。

### 7.2 来源确认

实施前从原理图、BOM和器件资料确认：

- 分流电阻阻值。
- 电流采样放大倍数。
- ADC参考电压和有效位数。
- 电流极性。
- 母线电压分压比。
- ADC1/ADC2通道与相电流的对应关系。

### 7.3 上板步骤

1. PWM和EN_GATE保持关闭。
2. 连续记录两相电流原始值不少于10秒。
3. 重复执行多次零偏校准，记录均值、最小值、最大值和标准差。
4. 低压下施加已知小电流，与电源或电流探头对照极性和比例。
5. 测量ADC注入中断频率及其相对PWM触发位置。
6. 检查普通组采样频率和数据更新。

### 7.4 通过标准

- PWM关闭时校准值稳定，重复校准差异处于预定阈值内。
- 两相零偏扣除后平均值接近零，无明显持续漂移。
- 电流方向与相定义一致。
- 注入采样稳定为目标10 kHz，无转换失败持续累积。
- 母线电压换算与仪表测量误差达到设定目标。

### 7.5 提交节点

```text
feat: add motor adc port and current offset calibration
```

## 8. M3：Hall反馈

### 8.1 开发内容

新增：

```text
project/bsp/motor_hall_port.c/.h
project/foc/foc_kernel/hall_estimator.c/.h
```

实现：

- PB5/PB6/PB7状态读取，固定 `bit0=HA`、`bit1=HB`、`bit2=HC`。
- 三路双边沿中断。
- Hall状态表、合法状态和合法相邻跳变检查。
- 正反转方向识别。
- DWT边沿时间戳和速度估计。
- 停转超时。
- 扇区内电角度插值。
- Hall映射和电角度偏移配置接口。

### 8.2 上板步骤

1. PWM关闭，手动缓慢正转电机一圈，记录Hall序列。
2. 手动反转一圈，记录反向序列。
3. 检查每个机械周期的边沿数与电机极对数关系。
4. 分别制造或模拟 `000`、`111`、跳扇区和抖动，检查诊断计数。
5. 使用稳定外部驱动或手转不同速度，对比Hall速度估计与参考速度。
6. 停止电机，验证超时后速度归零。
7. 与旧工程同一电机的Hall序列和角度方向进行对照。

### 8.3 通过标准

- 正反转均完整捕获六个合法状态，不漏下降沿。
- 方向判断与机械方向一致。
- 非法状态和非法跳变不会更新有效角度。
- 停转后在设定超时时间内速度归零。
- 电角度连续，无错误的整周反跳。
- Hall表和偏移没有明确来源时，不允许进入闭环阶段。

### 8.4 提交节点

```text
feat: add hall input port and estimator
```

## 9. M4：FOC数学内核

### 9.1 开发内容

新增：

```text
project/foc/foc_kernel/foc_types.h
project/foc/foc_kernel/foc_math.c/.h
project/foc/foc_kernel/foc_transform.c/.h
project/foc/foc_kernel/foc_pi.c/.h
project/foc/foc_kernel/foc_svpwm.c/.h
```

迁移原则：

- 旧 `foc_kernel.c` 仅作为计算行为来源。
- 函数和变量全部按新规范命名。
- 去除全局控制器、寄存器访问、打印和测试状态。
- 数值制式按架构评审结果实现。
- 每个算法明确输入范围、输出范围、比例和溢出策略。

### 9.2 离线测试向量

- Clarke：平衡三相、单相正负极值和零输入。
- Park/反Park：0°、90°、180°、270°及象限边界。
- Park往返：允许误差范围内恢复原矢量。
- PI：零误差、阶跃、饱和、解除饱和、正负限幅。
- SVPWM：六扇区边界、零矢量、最大矢量和越界输入。
- 角度回绕：`0xFFFF → 0x0000`及反向回绕。

旧工程和新内核使用同一输入向量，保存输出差异。若因修复旧缺陷而不一致，必须单独记录原因和预期结果。

### 9.3 通过标准

- 全部测试向量通过。
- 不存在未定义移位、有符号溢出和除零路径。
- 算法文件不包含任何AT32或WorkBench头文件。
- 10 kHz目标平台执行时间具有足够裕量；先记录各模块周期数，再确定预算。

### 9.4 提交节点

```text
feat: add verified foc math kernel
```

## 10. M5：受限开环

### 10.1 开发内容

- 建立 `motor_control.c/.h` 多速率入口。
- 建立最小 `motor_config.c/.h`。
- 实现固定电角度和低速旋转电角度两种开环测试。
- 建立母线电压、输出电压和占空比限制。
- 开环命令只能在明确的测试状态使用。

### 10.2 上板前条件

- M1至M4全部通过。
- 相序、PWM通道、ADC电流极性和Hall顺序已有记录。
- 电源设置为低电压、低限流。
- 机械负载脱开，电机固定可靠。
- 紧急停止手段已验证。

### 10.3 上板步骤

1. 不接电机，验证三相占空比和SVPWM扇区波形。
2. 接电机，固定角度施加极小电压矢量，观察转子对齐方向和相电流。
3. 低速正向旋转电角度，观察相序和电流。
4. 低速反向旋转，确认方向对称。
5. 将开环角度与Hall状态同步记录，形成Hall电角度校准依据。
6. 逐步增加但不超过批准限制，检查母线电流和器件温升。

### 10.4 通过标准

- 三相波形顺序正确，无上下桥异常导通。
- 正反向旋转方向可预测且一致。
- 固定角度时电流受限，无持续异常增长。
- Hall序列与开环电角度关系可重复。
- 任意异常均能立即关闭PWM和EN_GATE。

### 10.5 提交节点

```text
feat: add current-limited open-loop motor control
```

## 11. M6：Id/Iq电流闭环

### 11.1 开发内容

新增 `foc_current_loop.c/.h`，实现：

- 电流采样到dq反馈。
- Id和Iq参考值限制。
- 双PI、抗积分饱和和输出矢量限幅。
- 母线电压参与电压限制。
- PWM命令生成。
- 停止和故障时PI状态复位。
- 10 kHz运行时间统计，仅保存计数，不在ISR打印。

### 11.2 上板步骤

1. 锁轴或按批准的低能量方式测试，`id_ref=0`。
2. 给出小幅正负 `iq_ref` 阶跃，记录Ia/Ib、Id/Iq反馈、PI输出和母线电流。
3. 检查参考值为零时的静态电流。
4. 逐步提高电流参考，不超过阶段限制。
5. 测试输出饱和后解除，观察积分器恢复。
6. 在运行中触发软件限流和紧急停止。
7. 测量ADC ISR平均和最大执行周期。

### 11.3 通过标准

- Id保持在目标附近，Iq跟踪方向和幅值正确。
- 正负Iq响应基本对称。
- 饱和后无明显积分失控。
- 电流限制和紧急关闭有效。
- 10 kHz ISR最坏执行时间低于控制周期，并保留批准的安全裕量。
- 无持续过流、异常发热或母线电压异常。

### 11.4 提交节点

```text
feat: close id iq current loops
```

## 12. M7：速度闭环

### 12.1 开发内容

- Hall速度滤波。
- 速度参考斜坡。
- 速度PI输出到Iq参考。
- 速度、电流和加速度限制。
- 低速Hall分辨率不足时的明确策略。

### 12.2 上板步骤

1. 空载低速正转，逐级提高速度。
2. 空载低速反转。
3. 测试零速、低速、目标工作速度和速度阶跃。
4. 施加可控负载，观察速度恢复和Iq限制。
5. 测试Hall长时间无边沿、非法跳变和方向突变。

### 12.3 通过标准

- 正反向速度跟踪正确。
- 速度参考斜坡限制有效。
- 负载变化时Iq不超过限制。
- Hall低速抖动不会导致危险转矩跳变。
- 反馈丢失时进入安全状态。

### 12.4 提交节点

```text
feat: close hall speed loop
```

## 13. M8：位置闭环

### 13.1 入口条件

- 明确Hall是否能够满足目标位置精度。
- 明确机械传动比、位置单位、软限位和零点来源。
- 确定位置环目标频率。

### 13.2 开发内容

- 连续机械位置和圈数管理。
- 位置参考斜坡或轨迹限制。
- 位置环输出到速度参考。
- 正负软限位。
- 回绕、掉电后零点和重新归零策略。

### 13.3 上板步骤

- 小角度正负点动。
- 多圈正反向运行和回零。
- 跨角度回绕边界。
- 到达正负软限位。
- 运行中停止和重新使能。
- 外力扰动后的恢复。

### 13.4 通过标准

- 位置方向、单位和零点定义一致。
- 多圈累计无错误跳变。
- 软限位不可由普通命令越过。
- Hall精度无法满足目标时，明确停止该阶段，不以调PI掩盖传感器限制。

### 13.5 提交节点

```text
feat: close position loop with software limits
```

## 14. M9：状态机与必要故障

### 14.1 开发内容

新增：

```text
motor_state.c/.h
motor_fault.c/.h
motor_command.c/.h
```

实现最小状态：

```text
BOOT
SELF_TEST
CALIBRATING
READY
RUNNING
STOPPING
FAULT
```

第一阶段必要故障：

- 驱动器故障输入。
- 软件过流。
- 母线过压/欠压。
- Hall非法状态、非法连续跳变或反馈超时。
- ADC采样异常。
- 控制ISR超时。
- 参数无效。

### 14.2 故障注入验证

- 分别注入每种故障，确认PWM和EN_GATE动作。
- 验证故障锁存、错误码和清除条件。
- 验证运行中普通停止与紧急停止路径不同。
- 验证故障未解除时不能重新启动。
- 快速重复启停，检查PI和参考状态是否正确复位。

### 14.3 通过标准

- 只有状态机能够正常开启PWM。
- 严重故障不依赖主循环即可关闭输出。
- 每种故障有唯一、可追踪的错误码。
- 清除故障不会绕过物理条件检查。

### 14.4 提交节点

```text
feat: add motor state machine and essential protection
```

## 15. M10：Flash参数存储

### 15.1 开发内容

新增：

```text
project/bsp/flash_storage_port.c/.h
project/foc/foc_app/motor_config_storage.c/.h
```

同步修改App和Bootloader共享布局：

```text
参数区：0x0807F000 - 0x0807FFFF，共4 KB
Slot A：0x0807F000 - 0x0807F7FF
Slot B：0x0807F800 - 0x0807FFFF
```

实现：

- 固定区域边界检查。
- A/B槽扫描和有效性判断。
- 记录头、序号、长度、CRC和提交标记。
- 参数块编码、解码和版本处理。
- 缺失参数使用默认值。
- 未知参数块跳过。
- 保存只允许在安全状态执行。
- 明确是否需要一次性转换旧 `FlashSavedData`。

### 15.2 软件验证

- 空Flash加载默认值。
- Slot A有效、Slot B空。
- Slot B有效、Slot A旧。
- 两槽有效且序号不同。
- 记录头损坏、负载CRC损坏、长度越界、未知参数块。
- 序号回绕。
- 新固件读取少字段旧版本。
- 旧固件不要求读取新格式，除非另有兼容需求。

### 15.3 上板及掉电验证

1. 写入配置A并重启，确认一致。
2. 写入配置B并重启，确认选择新序号。
3. 在擦除新槽后掉电，确认旧槽仍可加载。
4. 在写记录头、写负载和写提交标记前分别模拟掉电。
5. 人工破坏CRC，确认回退到另一槽或默认值。
6. 连续保存多轮，检查A/B切换和Flash边界。
7. 运行状态请求保存，确认被拒绝且PWM不受影响。

### 15.4 通过标准

- 任一单次保存中断不会同时破坏两份有效配置。
- 无效记录永远不会被作为有效参数加载。
- 参数区写入不会越过 `0x0807F000–0x0807FFFF`。
- Bootloader OTA擦除和复制不会覆盖参数区。
- 参数错误不会导致自动反复擦写Flash。
- 保存失败后运行时参数和旧有效槽保持可用。

### 15.5 提交节点

```text
feat: add versioned dual-slot motor config storage
```

该节点涉及共享Flash布局，提交前必须同时评审App和Bootloader改动。

## 16. M11：系统回归与整理

### 16.1 回归项目

- 冷启动、热复位、重复下载和连续启停。
- ADC零偏、Hall方向和PWM相序。
- 开环、电流环、速度环和位置环。
- 全部必要故障注入。
- 参数保存、加载、损坏和掉电恢复。
- 最低和最高批准母线电压。
- 空载和批准负载。
- 长时间运行温升和错误计数。
- ISR最坏执行时间和主循环活性。

### 16.2 代码整理

- 搜索并删除失效旧命名和未使用接口。
- 检查不存在 `foc_fast`、旧 `ControllerStruct` 和旧函数兼容宏。
- 检查FOC内核无硬件头文件和寄存器访问。
- 检查Flash地址只有一个共享来源。
- 检查所有配置项单位和来源。
- 检查固件、硬件、参数格式和Bootloader版本没有混用。
- 检查发布版本、README、Git提交、Git标签和固件产物名称一致。
- 更新README修改记录和最终架构文档。

### 16.3 通过标准

- 所有阶段测试项具有可追溯记录。
- 全量构建无error，新增代码无warning。
- 不存在未解释的硬件异常计数。
- 安全限制和故障关闭路径全部回归通过。
- 生成可标记的稳定重构版本。

### 16.4 提交节点

```text
refactor: complete minimal hall foc control stack
```

建议在该提交上创建与固件版本一致的标签，例如 `fw-2026.09.03.0`。

## 17. 暂停与回退条件

出现以下任一情况，立即停止增加能量或闭环调试：

- PWM上电或复位时出现非预期脉冲。
- 上下桥驱动、死区或相序不确定。
- ADC电流极性或比例不确定。
- Hall状态顺序、极对数或角度偏移不确定。
- 母线电流超过阶段限制。
- 功率器件、电机或采样电阻异常升温。
- FOC ISR超过周期预算或持续出现ADC触发失败。
- 状态机无法可靠关闭PWM和EN_GATE。

处理方式：

1. 立即关闭PWM和门极，降低或断开母线电源。
2. 保存示波器波形、日志、固件提交和测试条件。
3. 回到最后一个已验证提交复测，区分硬件、配置和软件变化。
4. 根因确认并增加对应回归项后，才允许继续下一阶段。

## 18. 待确认事项

### 18.1 已完成验证（2026.09.03.4）

- ADC零偏1024点阻塞校准通过，典型零偏约2060/2061。
- 10 kHz快速ADC、1 kHz慢速DMA及20 kHz TMR1通道4频率符合设计，ADC触发失败为0。
- 三相50% PWM、DRV8353唤醒、nFAULT/BIF清除和紧急关闭通过上板验证。
- 10 Hz低压开环试转稳定，Hall三路各20边沿/秒，正向六步无非法及重复跳变。
- Hall插值角度与开环角度误差约0.5°，满足进入电流坐标变换验证的条件。
- 电流极性、Clarke/Park和整周期dq统计通过；正Uq对应正Iq。
- 阻塞式参数辨识得到Rs=273 mΩ、Ld=225 µH、Lq=206 µH。

### 18.2 控制模块规范化状态

- 已建立正式可配置开环接口，运行指令包含Vd、Vq、目标电频率和加速度。
- 已将有符号斜坡、dq联合限幅下沉到无硬件依赖的`foc_kernel`。
- 已建立正式`motor_current_control`，统一管理电流闭环计算、PWM和故障停机。
- 转矩测试已迁移为正式控制器的调用者，仅保留起转、交接、斜坡和统计职责。
- 本轮结构调整已通过ARMCC逐文件编译，尚未执行调整后的上板回归测试。

### 18.3 USART1命令链路待验证项

- 上电日志结束后应进入READY，且不自动开启PWM或执行辨识、试转。
- 921600 8N1下依次验证`help`、`version`、`status`、`log level`和`motor stop`。
- 验证USART1 IDLE分帧、连续多行、超长行以及单帧邮箱溢出计数。
- 架空并限流后验证`open start/set/stop`，再验证Hall有效时`current start/set/stop`无扰接管。
- 人为触发安全故障后验证`fault`和`fault clear`的拒绝及恢复条件。
- 200 Hz电流PI固定角度测试通过：Id指令2000 mA、平均1991 mA，Iq平均-12 mA，峰值低于10 A限制。
- 当前快速中断最坏负载约21.7%，仍有控制功能扩展余量。

开始M0前确认：

- 核心数值制式是否先沿用旧整数/Q格式。
- 第一阶段是否只支持Hall，不迁移DPT。
- 电机型号、极对数及参数来源。
- 旧Flash参数是否需要一次性转换。
- 文件头中的作者和初始版本规则。
- 当前PCB/PCBA正式硬件版本号及其来源。
- Bootloader当前版本和最低兼容版本。

进入M5前确认：

- 初次上电母线电压和电源限流值。
- 最大允许开环电压/占空比。
- 紧急停止的人员操作和硬件手段。

进入M6至M8前分别确认：

- 电流、速度、位置的阶段限制。
- 负载条件和机械安全范围。
- 各环路目标带宽、超调和稳态误差验收指标。
