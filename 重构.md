重构代码：
保留WorkBench的重新生成能力，代码新增在用户代码区或者新增文件
新增文件的职能要清晰，标注好信息，例如（/**
 * @file    foc_controller.h
 * @brief   FOC控制器公共配置、运行状态、数据结构和接口声明
 * @author  随永祥
 * @date    2025-08-01
 * @version 1.0
 */）
 设计架构、方案清晰后，再逐步实施，边改边验证，关键节点要提醒提交
 E:\src\AT_foc_hall - 副本，原工程作为参考，设计不猜测要有来源

## 配置层约定

自维护配置统一放在`project/config`：`motor_control_config.h`管理电机与控制参数，`motor_board_config.h`管理板级硬件映射，
`product_version.h`管理软硬件版本，`ota_config.h`管理Bootloader/App共享Flash布局。WorkBench生成的配置继续保留在`project/inc`和`project/src`。
关键控制参数必须提供编译期一致性检查，禁止以运行时容错替代明显的静态配置错误。

## 当前速度闭环设计

Hall端口在每个合法边沿更新测速，最近6段间隔构成滑动窗口；机械转速按4对极换算，1 kHz速度任务维护滤波、斜坡、PI和保护，
速度PI输出Iq mA指令给10 kHz电流环。串口对外统一使用整数rpm，内部使用milli-rpm。当前只允许正向，Iq限制±2 A，
包含Hall失效、反向、堵转和超速保护。该链路已通过ARMCC逐文件编译，尚未完成速度闭环上板验证。
